#use std_core::io::IO;
#use std_core::primitive::i32::i32;

#fn write_value : (value : i32) -> (value : i32) -> () #with IO {
    #let _ : () <- IO::sys_exit value;
    ()
}

#entrypoint main;

#fn main : () #with IO {
    ()
}
