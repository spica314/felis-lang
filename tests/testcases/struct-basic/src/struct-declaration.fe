#use std_core::io::IO;
#use std_core::primitive::i32::i32;

#struct Span : Type[0] {
    start : i32,
    end : i32,
}

#struct(rc) Point : Type[0] {
    x : i32,
    y : i32,
}

#entrypoint main;

#fn main : () #with IO {
    #let _ <- IO::exit 42i32;
    ()
}
