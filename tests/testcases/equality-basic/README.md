Fixture package for pure equality functions that return `bool`.

It covers nested `#match` equality over a nullary enum, nested `#match`
equality over a payload enum, and direct field equality over a struct.
