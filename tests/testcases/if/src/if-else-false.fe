#use std_core::io::IO;
#use std_core::primitive::i32::i32_eq;

#entrypoint main;

#fn main : () #with IO {
    #if i32_eq 3i32 4i32 {
        #let _ : () <- IO::sys_exit 1i32;
    } #else {
        #let _ : () <- IO::sys_exit 42i32;
    };
    ()
}
