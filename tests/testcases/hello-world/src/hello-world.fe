#use std_core::io::IO;
#use std_core::io::FileDescriptor;

#entrypoint main;

#fn main : () #with IO {
    #let stdout : FileDescriptor <- IO::stdout;
    #let hello_world_ref : Slice u8 = "Hello, world!\n";
    #let _ : () <- IO::write stdout hello_world_ref 14i32;
    ()
}
