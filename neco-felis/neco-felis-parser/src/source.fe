#use std_core::primitive::array::Slice;
#use std_core::primitive::i32::i32;

#struct ParsedSourceFile : Type[0] {
    exit_code : i32,
}

#fn parse_source : (source : Slice u8) -> ParsedSourceFile {
    ParsedSourceFile { exit_code = parse_exit_code source }
}
